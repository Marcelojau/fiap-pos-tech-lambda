name: Create Pull Request Develop To Release

on:
  push:
    branches:
      - release

jobs:
  create_pull_request:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do Código
        uses: actions/checkout@v2

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v3
        with:
          title: "Merge release into develop"
          body: "This pull request was automatically created to merge changes from release into main."
          base: release
          branch: main
          labels: "auto-generated"